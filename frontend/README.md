# Patient web (`frontend/`) — legacy / non-production

> **Not a shipping client.** Production patient app is [`flutter_mobile/`](../flutter_mobile/).  
> Ops / staff UI is [`admin/`](../admin/). API is [`fastapi_back/`](../fastapi_back/).

This React + Vite portal remains for historical UI and limited local demos. It does **not** include Flutter-only features (Health Community, Health Protection, enterprise AI assistant).

## Support contact (canonical)

- Email: `medichain123@gmail.com` (`SUPPORT_EMAIL`)
- Phone: `1800-123-4567` (`SUPPORT_PHONE`)

## Run locally

```bash
cd frontend
npm install
# Set VITE_BACKEND_URL in .env (e.g. http://localhost:5000)
npm run dev
```
