# Prometheus scrape config for local MedClues API
global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/slo_rules.yml

scrape_configs:
  - job_name: medclues-api
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:5000"]
