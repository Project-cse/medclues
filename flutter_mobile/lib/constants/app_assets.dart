/// Central asset paths — change filenames here when you swap banner or splash media.
class AppAssets {
  AppAssets._();

  /// Doctor banner illustration (homepage hero banner, right side).
  static const String bannerNavigation = 'assets/images/banner_navigation.png';

  /// Opening intro video (splash screen). Replace the file at this path to change the video.
  static const String splashVideo = 'assets/videos/opening.mp4';
}
