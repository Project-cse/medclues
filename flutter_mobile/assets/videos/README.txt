opening.mp4 — MEDCLUES splash video

BEST RESULT (seamless full-screen, no visible frame):
  Export as 1080 x 1920 portrait (9:16)
  Background #f5f5f5 must extend to ALL edges of the frame (no inner padding/card)
  H.264 MP4, 2-4 seconds
  Keep logo in center safe zone

If your video is landscape (16:9), the app scales with BoxFit.cover on phones
so the video background fills the screen — re-export as 9:16 for perfect match.

After replacing the file: full restart (flutter run), not hot reload.
